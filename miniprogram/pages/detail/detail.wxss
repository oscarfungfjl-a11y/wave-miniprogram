.spot-title {
  font-size: 38rpx;
  font-weight: 700;
  color: #24292F;
  letter-spacing: -0.02em;
}

.spot-meta {
  margin-top: 8rpx;
}

/* 推荐摘要卡片 */
.rec-score-hero {
  display: flex;
  align-items: baseline;
  margin-bottom: 20rpx;
}

.rec-big-score {
  font-size: 64rpx;
  font-weight: 700;
  color: #DC9018;
  line-height: 1;
}

/* 四维数据卡片 */
.rec-dims {
  display: flex;
  gap: 12rpx;
  margin-bottom: 14rpx;
}

.rec-dim {
  flex: 1;
  text-align: center;
  padding: 16rpx 8rpx;
  background: rgba(13,132,150,0.06);
  border-radius: 12rpx;
}

.rec-dim-label {
  display: block;
  font-size: 22rpx;
  color: #9BA5AF;
  margin-bottom: 4rpx;
  text-transform: uppercase;
  letter-spacing: 0.04em;
}

.rec-dim-val {
  display: block;
  font-size: 28rpx;
  font-weight: 600;
  color: #0D8496;
}

.rec-best-hour {
  padding: 14rpx 0;
  text-align: center;
  color: #59616B;
}

/* 挡位徽章 */
.tier-badge {
  padding: 5rpx 16rpx;
  border-radius: 20rpx;
  font-weight: 600;
  font-size: 24rpx;
  letter-spacing: 0.03em;
}

.tier-high { background: #3D8B5E; color: #FFFFFF; }
.tier-mid  { background: #DC9018; color: #FFFFFF; }
.tier-low  { background: #CD4035; color: #FFFFFF; }

/* 小时预报表格 */
.h-table {
  border-radius: 16rpx;
  overflow: hidden;
  background: rgba(255,255,255,0.78);
  border: 1px solid rgba(255,255,255,0.55);
  box-shadow: 0 4px 16px rgba(0,0,0,0.03);
}

.hourly-list {
  max-height: 560rpx;
  overflow-y: auto;
}

.h-thead {
  display: flex;
  align-items: center;
  padding: 16rpx 14rpx;
  background: rgba(13,132,150,0.06);
  border-bottom: 1px solid rgba(0,0,0,0.06);
}

.h-th {
  font-size: 22rpx;
  color: #9BA5AF;
  font-weight: 600;
  text-align: center;
  flex-shrink: 0;
  letter-spacing: 0.02em;
}

.h-tr {
  display: flex;
  align-items: center;
  padding: 18rpx 14rpx;
  border-bottom: 1px solid rgba(0,0,0,0.04);
}

.h-tr:nth-child(even) {
  background: rgba(13,132,150,0.02);
}

.h-td {
  font-size: 24rpx;
  flex-shrink: 0;
}

/* 列宽 */
.h-th-time, .h-td-time {
  width: 76rpx;
  text-align: left;
  color: #9BA5AF;
  padding-left: 4rpx;
}

.h-th-wave, .h-td-wave {
  width: 84rpx;
  text-align: right;
  padding-right: 8rpx;
  font-weight: 600;
}

.h-th-per, .h-td-per {
  width: 72rpx;
  text-align: right;
  padding-right: 8rpx;
  color: #59616B;
}

.h-th-dir, .h-td-dir {
  width: 80rpx;
  text-align: center;
}

.h-th-temp, .h-td-temp {
  width: 72rpx;
  text-align: right;
  padding-right: 8rpx;
  color: #59616B;
}

.h-th-tide, .h-td-tide {
  width: 88rpx;
  text-align: right;
  padding-right: 8rpx;
  color: #59616B;
}

.h-th-pts, .h-td-pts {
  width: 56rpx;
  text-align: right;
  padding-right: 4rpx;
  font-weight: 600;
}

/* 评分颜色 */
.rec-score-high { color: #3D8B5E; }
.rec-score-mid  { color: #DC9018; }
.rec-score-low  { color: #CD4035; }

/* 未来天 */
.future-day-header {
  padding: 18rpx 24rpx;
  background: linear-gradient(135deg, #E8F5F8, #D4EEF3);
  border-radius: 16rpx 16rpx 0 0;
  margin-top: 8rpx;
  font-weight: 600;
  color: #0D8496;
}
